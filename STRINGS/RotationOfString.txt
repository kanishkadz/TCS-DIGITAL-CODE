class Main {
    public static void main(String[] args) {
        String str1 = "abcde";
        String str2 = "cdeab";

        boolean isRotation = (str1.length() == str2.length()) && ((str1 + str1).contains(str2));

        System.out.println(isRotation ? "Rotation" : "Not Rotation");
    }
}
